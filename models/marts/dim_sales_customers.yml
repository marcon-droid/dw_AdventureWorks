version: 2

models:
  - name: dim_sales_customers
    columns:
      - name: customerid_sk
        description: The primary key of the customers 
        tests:
          - unique
          - not_null