version: 2

models:
  - name: fact_orders
    columns:
      - name: salesorderdetailid
        description: The primary key of the order-item
        tests:
          - unique
          - not_null