version: 2

models:
  - name: dim_sales_reason
    columns:
      - name: salesID_sk
        description: The primary key of the sales reason
        tests:
          - unique
          - not_null